<% if current_user.follows?(User.find_by(id: params[:id])) %>
  <% follow_attribute = "followed" %>
<% else %>
  <% follow_attribute = "unfollowed" %>
<% end %>

<button class="follow-toggle" data-initial-follow-state=<%= follow_attribute %> data-user-id=<%= params[:id] %>></button>
